/** @jsx jsx */
import { jsx } from '@emotion/core';

const style = {
  position: 'fixed', 
};

const SideMenu = () => {
  return (
    <section css={style}>
    </section>
  );
};

export default SideMenu;
