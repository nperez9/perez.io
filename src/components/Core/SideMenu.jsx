/** @jsx jsx */
import { jsx } from '@emotion/core';

const style = {
  position: 'fixed', 
};

const SideMenu = () => {
  return (
    <section css={style}>
      <div>
        this is test
      </div>
    </section>
  );
};

export default SideMenu;
